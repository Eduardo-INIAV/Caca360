<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:caca360.ViewModels"
             xmlns:vm="clr-namespace:caca360" 
             x:Class="caca360.MainPage"
             x:DataType="viewModels:MainViewModel"
             Title="MainPage">

    <VerticalStackLayout Padding="20" Spacing="15">
        <Label Text="Bem-vindo ao Caça 360" FontSize="24" HorizontalOptions="Center" VerticalOptions="Center" />
        <Button Text="Ir para Dashboard" Command="{Binding NavigateToDashboardCommand}" />
        <Button Text="Ir para Abate" Command="{Binding NavigateToAbateCommand}" />
        <Button Text="Ir para Censos" Command="{Binding NavigateToCensosCommand}" />
        <Button Text="Ir para Profile" Command="{Binding NavigateToProfileCommand}" />
    </VerticalStackLayout>
</ContentPage>