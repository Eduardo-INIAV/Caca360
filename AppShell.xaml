<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:caca360"
       x:Class="caca360.AppShell">

    <TabBar>
        <ShellContent Title="Dashboard" ContentTemplate="{DataTemplate local:DashboardPage}" />
        <ShellContent Title="Abate" ContentTemplate="{DataTemplate local:AbatePage}" />
        <ShellContent Title="Censo" ContentTemplate="{DataTemplate local:CensosPage}" />
    </TabBar>
</Shell>
